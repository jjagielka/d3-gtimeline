var tape = require("tape"),
    timeline = require("../");

tape("timeline() returns the answer to the ultimate question of life, the universe, and everything.", function(test) {
  //test.equal(timeline.timeline(), 42);
  test.end();
});
